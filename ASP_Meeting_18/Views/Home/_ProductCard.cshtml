
@model Product
@*<div class="card mb-3" style="max-width: 540px;">
	<div class="row g-0">
		<div class="col-md-4">
			<img src="@Model.Photos.FirstOrDefault()!.PhotoUrl" class="img-fluid rounded-start" alt="@Model.Title">
		</div>
		<div class="col-md-8">
			<div class="card-body">
				<h5 class="card-title">@Model.Title</h5>
				<p class="card-text">@Model.Price.ToString("# EUR")</p>
				<p class="card-text"><small class="text-muted">@Model!.Category!.Title</small></p>
				<a asp-action="AddToCart" asp-controller="Cart" asp-route-id="@Model.Id" class="btn btn-success">Add to Cart</a>
			</div>
		</div>
	</div>
</div> *@
<div class="card mb-3" style="max-width: 540px;">
	<div class="row g-0">
		<div class="col-md-4">
			<img src="@Model.Photos.FirstOrDefault()!.PhotoUrl" class="img-fluid rounded-start" alt="@Model.Title">
			</div>
		<div class="col-md-8">
			<div class="card-body">
				<h5 class="card-title">@Model.Title</h5>
				<p class="card-text">
					@Model.Price.ToString("# $")</p>
					@*<form asp-action="AddToCart" asp-controller="Cart" method="post">
						<input type="hidden" value="@(Context.Request.Path.Value+Context.Request.QueryString.Value)" name="returnUrl" />
						<input type="hidden" value="@(Model.Id)" name="id" />
						<input type="submit" value="Add to Cart" class="btn btn-sm btn-success float-end"/>
					</form>*@
				<p class="card-text"><small class="text-muted">@Model!.Category!.Title</small></p>
				<a asp-action="Details" asp-controller="Products" asp-route-id="@Model.Id" class="btn btn-sm btn-success">Details</a>
				@*<a asp-action="AddToCart" asp-controller="Cart" asp-route-id="@Model.Id" class="btn btn-success">Add to Cart</a>*@
			</div>
		</div>
	</div>
</div>